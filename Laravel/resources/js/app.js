import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './react/App'; // Asegúrate de que el path esté correcto

const root = ReactDOM.createRoot(document.getElementById('root')); // Se asegura de que el root esté bien
root.render(<App />);
